server {
    listen 80;
    root /usr/share/nginx/html;
    index index.html;

    # 🔥 Route all unknown paths to index.html (Fix refresh issue)
    location / {
        try_files $uri /index.html;
    }
}
